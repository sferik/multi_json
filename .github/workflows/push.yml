name: Push gem to RubyGems

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  push:
    if: github.repository == 'sferik/multi_json'
    runs-on: ubuntu-latest
    environment:
      name: rubygems.org
      url: https://rubygems.org/gems/multi_json
    permissions:
      contents: write
      id-token: write

    strategy:
      matrix:
        include:
          - ruby: ruby
            platform: ""
          - ruby: jruby
            platform: java

    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
      - uses: rubygems/configure-rubygems-credentials@v1.0.0
      - name: Update RubyGems
        run: gem update --system
      - name: Build gem
        run: gem build multi_json.gemspec
        env:
          MULTI_JSON_PLATFORM: ${{ matrix.platform }}
      - name: Sign gem with Sigstore
        run: |
          for gem in multi_json-*.gem; do
            gem exec sigstore-cli sign "$gem" --bundle "${gem%.gem}.gem.sigstore.json"
          done
      - name: Push gem
        run: |
          for gem in multi_json-*.gem; do
            gem push "$gem" --attestation "${gem%.gem}.gem.sigstore.json"
          done
      - name: Wait for release
        run: gem exec rubygems-await multi_json-*.gem
        env:
          MULTI_JSON_PLATFORM: ${{ matrix.platform }}
      - name: Create GitHub release
        if: matrix.ruby == 'ruby'
        run: |
          tag_name="$(git describe --tags --abbrev=0)"
          gh release create "${tag_name}" --verify-tag --generate-notes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
